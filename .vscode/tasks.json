{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build philosphers",
      "type": "shell",
      "command": "make",
      "args": ["-j", "8"],            // optional – parallel build
      "options": { "cwd": "${workspaceFolder}" },
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": ["$gcc"]      // lets VS Code jump to errors
    }
  ]
}